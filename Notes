Backend
    remove old Gemfile.lock to prevent deprecated packages from causing problems
    bundle install

    sudo apt-get install postgresql
    sudo service postgresql start
    sudo -u postgres createuser -s yuci

    rails db:reset
    rails s

Frontend
    npm run start

Heroku
    heroku login
    heroku run rails db:migrate -a slock-app
    heroku run rails db:seed -a slock-app

aws 
    EDITOR="code --wait" rails credentials:edit
    EDITOR="code --wait" rails credentials:show
    heroku config:set RAILS_MASTER_KEY=$(cat config/master.key) -a your-app-name